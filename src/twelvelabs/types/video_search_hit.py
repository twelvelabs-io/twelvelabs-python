# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .video_match import VideoMatch
from .video_search_item_metadata import VideoSearchItemMetadata


class VideoSearchHit(UniversalBaseModel):
    """
    A search result for a video.
    """

    rank: int = pydantic.Field()
    """
    The relevance position of this result, starting at 1. When `group_by` is `item`, videos are ordered by their most relevant clip.
    """

    item_id: str = pydantic.Field()
    """
    The unique identifier of the knowledge store item.
    """

    metadata: typing.Optional[VideoSearchItemMetadata] = pydantic.Field(default=None)
    """
    Metadata attached to the item. Returned when you set the `include_metadata` parameter to `true`.
    """

    matches: typing.List[VideoMatch] = pydantic.Field()
    """
    Matching clips from this video, ordered by relevance.
    
    - When `group_by` is `none`: Contains one entry — the matching clip.
    - When `group_by` is `item`: Contains all matching clips from this video, with the best match first.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
