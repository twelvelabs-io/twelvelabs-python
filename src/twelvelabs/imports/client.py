# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.import_detail import ImportDetail
from ..types.import_result import ImportResult
from .raw_client import AsyncRawImportsClient, RawImportsClient
from .types.import_files_request_items_item import ImportFilesRequestItemsItem
from .types.list_imports_response import ListImportsResponse

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ImportsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawImportsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawImportsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawImportsClient
        """
        return self._raw_client

    def list_imports(
        self,
        connection_id: str,
        *,
        page: typing.Optional[int] = None,
        page_limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListImportsResponse:
        """
        This method returns a list of the imports for the specified connection. The platform returns the imports sorted by creation date, with the newest at the top of the list. Each import in the list is a summary that omits the `items` array. To see the status of each file, use the [Retrieve an import](/v1.3/api-reference/data-connectors/imports/retrieve-an-import) endpoint.

        Parameters
        ----------
        connection_id : str
            The unique identifier of the connection to list imports for.

        page : typing.Optional[int]
            A number that identifies the page to retrieve.

            **Default**: `1`.

        page_limit : typing.Optional[int]
            The number of items to return on each page.

            **Default**: `10`.
            **Max**: `50`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListImportsResponse
            The imports have been successfully retrieved.

        Examples
        --------
        from twelvelabs import TwelveLabs

        client = TwelveLabs(
            api_key="YOUR_API_KEY",
        )
        client.imports.list_imports(
            connection_id="665f0a2c9b1e4d0012a3f7c9",
            page=1,
            page_limit=10,
        )
        """
        _response = self._raw_client.list_imports(
            connection_id, page=page, page_limit=page_limit, request_options=request_options
        )
        return _response.data

    def import_files(
        self,
        connection_id: str,
        *,
        items: typing.Sequence[ImportFilesRequestItemsItem],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ImportResult:
        """
        This method imports one or more files from the connected provider account into the platform as assets. Video files can be up to 10 GB, audio files up to 4 GB, and images up to 32 MB. Each newly imported file creates an asset in the `processing` status and is downloaded asynchronously. If you import a file that was already imported through this account, the platform returns the existing asset with its current status, which may be `ready`, without downloading the file again. The response returns one entry per requested file, in request order.

        Parameters
        ----------
        connection_id : str
            The unique identifier of the connection to import through.

        items : typing.Sequence[ImportFilesRequestItemsItem]
            The files to import. Provide an array of one item for a single import, or multiple items for a batch import. A maximum of 100 items can be imported per request. Each `source_id` must be unique within a request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportResult
            The import request has been accepted. The response returns one item per requested file, in request order. An accepted item includes the `asset_id` and `status` fields. A rejected item includes an `error` object instead and omits those fields. The `has_failures` field is `true` when any item was rejected.

        Examples
        --------
        from twelvelabs import TwelveLabs
        from twelvelabs.imports import ImportFilesRequestItemsItem

        client = TwelveLabs(
            api_key="YOUR_API_KEY",
        )
        client.imports.import_files(
            connection_id="665f0a2c9b1e4d0012a3f7c9",
            items=[
                ImportFilesRequestItemsItem(
                    source_id="1AbCDef_drive_file_id_x",
                )
            ],
        )
        """
        _response = self._raw_client.import_files(connection_id, items=items, request_options=request_options)
        return _response.data

    def retrieve_import(
        self, connection_id: str, import_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ImportDetail:
        """
        This method retrieves a single import, including the current status of each asset in the import.

        Parameters
        ----------
        connection_id : str
            The unique identifier of the connection the import belongs to.

        import_id : str
            The unique identifier of the import to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportDetail
            The import has been successfully retrieved.

        Examples
        --------
        from twelvelabs import TwelveLabs

        client = TwelveLabs(
            api_key="YOUR_API_KEY",
        )
        client.imports.retrieve_import(
            connection_id="665f0a2c9b1e4d0012a3f7c9",
            import_id="665f0afe9b1e4d0012a3f7d0",
        )
        """
        _response = self._raw_client.retrieve_import(connection_id, import_id, request_options=request_options)
        return _response.data


class AsyncImportsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawImportsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawImportsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawImportsClient
        """
        return self._raw_client

    async def list_imports(
        self,
        connection_id: str,
        *,
        page: typing.Optional[int] = None,
        page_limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ListImportsResponse:
        """
        This method returns a list of the imports for the specified connection. The platform returns the imports sorted by creation date, with the newest at the top of the list. Each import in the list is a summary that omits the `items` array. To see the status of each file, use the [Retrieve an import](/v1.3/api-reference/data-connectors/imports/retrieve-an-import) endpoint.

        Parameters
        ----------
        connection_id : str
            The unique identifier of the connection to list imports for.

        page : typing.Optional[int]
            A number that identifies the page to retrieve.

            **Default**: `1`.

        page_limit : typing.Optional[int]
            The number of items to return on each page.

            **Default**: `10`.
            **Max**: `50`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListImportsResponse
            The imports have been successfully retrieved.

        Examples
        --------
        import asyncio

        from twelvelabs import AsyncTwelveLabs

        client = AsyncTwelveLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.list_imports(
                connection_id="665f0a2c9b1e4d0012a3f7c9",
                page=1,
                page_limit=10,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_imports(
            connection_id, page=page, page_limit=page_limit, request_options=request_options
        )
        return _response.data

    async def import_files(
        self,
        connection_id: str,
        *,
        items: typing.Sequence[ImportFilesRequestItemsItem],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ImportResult:
        """
        This method imports one or more files from the connected provider account into the platform as assets. Video files can be up to 10 GB, audio files up to 4 GB, and images up to 32 MB. Each newly imported file creates an asset in the `processing` status and is downloaded asynchronously. If you import a file that was already imported through this account, the platform returns the existing asset with its current status, which may be `ready`, without downloading the file again. The response returns one entry per requested file, in request order.

        Parameters
        ----------
        connection_id : str
            The unique identifier of the connection to import through.

        items : typing.Sequence[ImportFilesRequestItemsItem]
            The files to import. Provide an array of one item for a single import, or multiple items for a batch import. A maximum of 100 items can be imported per request. Each `source_id` must be unique within a request.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportResult
            The import request has been accepted. The response returns one item per requested file, in request order. An accepted item includes the `asset_id` and `status` fields. A rejected item includes an `error` object instead and omits those fields. The `has_failures` field is `true` when any item was rejected.

        Examples
        --------
        import asyncio

        from twelvelabs import AsyncTwelveLabs
        from twelvelabs.imports import ImportFilesRequestItemsItem

        client = AsyncTwelveLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.import_files(
                connection_id="665f0a2c9b1e4d0012a3f7c9",
                items=[
                    ImportFilesRequestItemsItem(
                        source_id="1AbCDef_drive_file_id_x",
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.import_files(connection_id, items=items, request_options=request_options)
        return _response.data

    async def retrieve_import(
        self, connection_id: str, import_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ImportDetail:
        """
        This method retrieves a single import, including the current status of each asset in the import.

        Parameters
        ----------
        connection_id : str
            The unique identifier of the connection the import belongs to.

        import_id : str
            The unique identifier of the import to retrieve.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportDetail
            The import has been successfully retrieved.

        Examples
        --------
        import asyncio

        from twelvelabs import AsyncTwelveLabs

        client = AsyncTwelveLabs(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.retrieve_import(
                connection_id="665f0a2c9b1e4d0012a3f7c9",
                import_id="665f0afe9b1e4d0012a3f7d0",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retrieve_import(connection_id, import_id, request_options=request_options)
        return _response.data
